{
	"name": "{{PACKAGE_NAME}}",
	"version": "0.1.0",
	"description": "OpenCode plugin",
	"keywords": [
		"opencode",
		"opencode-plugin",
		"plugin"
	],
	"license": "MIT",
	"files": [
		"src"
	],
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"dev": "bun run dev.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@opencode-ai/plugin": "{{OPENCODE_PLUGIN_VERSION}}",
		"@opencode-ai/sdk": "{{OPENCODE_SDK_VERSION}}"
	},
	"devDependencies": {
		"@types/bun": "^1.3.8",
		"typescript": "^5.7.2"
	}
}
